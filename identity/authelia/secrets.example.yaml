totp:
  issuer: "example.com"


webauthn:
  display_name: "example.com"


authentication_backend:
  ldap:
    base_dn: "dc=example,dc=com"
    user: 'uid=authelia,dc=example,dc=com'
    password: 'password123'


session:
  secret: 'a_very_secret_session_key'
  cookies:
    domain: 'example.com'
    authelia_url: 'https://auth.example.com'


storage:
  encryption_key: 'a_very_secret_encryption_key'


identity_validation:
  reset_password:
    jwt_secret: 'a_very_secret_jwt_key'


identity_providers:
  oidc:
    hmac_secret: 'this_is_a_secret_abc123abc123abc'
    jwks:
      - algorithm: 'RS256'
        key: |
          -----BEGIN PRIVATE KEY-----
          ...
          -----END PRIVATE KEY-----
    clients:
      - id: 'example-client'