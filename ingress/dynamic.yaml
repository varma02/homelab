http:
  routers:
    truenas:
      rule: Host(`nas1.ts.varma01.dev`)
      entryPoints:
        - websecure
      service: truenas
      tls:
        certResolver: cf

services:
  truenas:
    loadBalancer:
      servers:
        - url: https://10.12.1.5:18443/